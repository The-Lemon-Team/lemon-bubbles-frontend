import {
  DEFAULT_FLOATING_HEIGHT,
  DEFAULT_FLOATING_WIDTH,
  DEFAULT_FLOATING_X,
  DEFAULT_FLOATING_Y,
} from '../../../constants';

export const sizes = {
  width: DEFAULT_FLOATING_HEIGHT,
  height: DEFAULT_FLOATING_WIDTH,
};

export const coordinates = { x: DEFAULT_FLOATING_X, y: DEFAULT_FLOATING_Y };

export const featureFlags = {
  floatingWindow: false,
};
